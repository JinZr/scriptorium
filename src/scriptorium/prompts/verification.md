Independently verify whether the approved patch resolves every confirmed finding without introducing factual,
numeric, citation, figure, or cross-section regressions. Use the patched navigation and sources, follow references
from each changed location, and inspect rendered pages for visual changes. Return pass only when the patched
manuscript is sound.
